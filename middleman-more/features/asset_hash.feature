Feature: Assets get a file hash appended to their and references to them are updated
  Scenario: Hashed-asset fiels are produced, and HTML, CSS, and JavaScript gets rewritten to reference the new files
    # Test HTML
    # Test CSS
    # Test JS
    # Test with relative_assets
    # Test with image_url, javascript_include_tag, stylesheet_link_tag
    # Test with image helper in SASSb
    # Test on file in a subdirectory
    # Test on directory indexes
    
  Scenario: Enabling an asset host still produces hashed files and references